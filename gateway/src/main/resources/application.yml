server:
  port: 8080

spring:
  application:
    name: ${project.artifactId}

project:
  artifactId: '@project.artifactId@'
  name: '@project.name@'
  version: '@project.version@'
  description: '@project.description@'
  groupId: '@project.groupId@'

jwt:
  secret: asecretindescifrable
